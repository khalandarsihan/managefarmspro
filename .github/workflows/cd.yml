name: CD

on:
  push:
    branches:
      - staging
  workflow_dispatch: # This allows manual triggering from the GitHub UI

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate Version Number
        run: |
          version=$(date +"%Y%m%d%H%M%S")
          echo "version=$version" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Tag Docker Image
        run: |
          docker build \
            --build-arg=FRAPPE_PATH=https://github.com/frappe/frappe \
            --build-arg=FRAPPE_BRANCH=version-15 \
            --build-arg=PYTHON_VERSION=3.11.9 \
            --build-arg=NODE_VERSION=18.20.2 \
            --build-arg=APPS_JSON_BASE64=${{ secrets.APPS_JSON_BASE64 }} \
            --tag=ghcr.io/${{ github.repository_owner }}/managefarmspro/frappe-app:${{ env.version }} \
            --file=images/custom/Containerfile .

      - name: Push Docker Image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/managefarmspro/frappe-app:${{ env.version }}

      - name: Set Deployment Environment Variables
        run: |
          echo "CUSTOM_IMAGE=ghcr.io/${{ github.repository_owner }}/managefarmspro/frappe-app" >> $GITHUB_ENV
          echo "CUSTOM_TAG=${{ env.version }}" >> $GITHUB_ENV

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Clean Up Docker on Frappe Server
        uses: appleboy/ssh-action@v0.1.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker system prune -af
            docker volume prune -f

      - name: SSH and Deploy Frappe App
        uses: appleboy/ssh-action@v0.1.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/sihan/staging/frappe_docker
            sed -i "s|image: .*/frappe-app:.*|image: ghcr.io/${{ github.repository_owner }}/managefarmspro/frappe-app:${{ env.version }}|g" pwd.yml
            docker compose -f pwd.yml down
            docker compose -f pwd.yml pull
            docker compose -f pwd.yml up -d
            sleep 300
            if curl -s -o /dev/null -w "%{http_code}" https://khasihan.xyz | grep 200; then
              echo "Deployment successful!"
            else
              echo "Deployment failed"
              exit 1
            fi

      - name: Finish CD
        if: success()
        run: echo "CD process completed."

